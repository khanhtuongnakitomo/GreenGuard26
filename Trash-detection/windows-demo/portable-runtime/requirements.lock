# Runtime dependency lock for the Windows x64 portable profile.
# These wheels must be staged by the release operator; the builder never
# downloads dependencies during an offline build.
numpy==2.4.6
opencv-python==5.0.0.93
onnx==1.22.0
onnxruntime==1.29.0
Pillow==12.3.0
torch==2.13.0
torchvision==0.28.0
ultralytics==8.4.128
